VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-publishable-key

# Server-side only (Netlify env var). Powers the AI email writer at /api/generate-email.
# Without it, the app falls back to the built-in template writer.
ANTHROPIC_API_KEY=your-anthropic-api-key
